{
  "name": "restaurant-operations-os",
  "private": true,
  "description": "HRCore — npm start → http://localhost:8080 (folder: restaurant-hr). Supabase Auth: Redirect URLs დაამატეთ http://localhost:8080/**",
  "scripts": {
    "start": "serve -l 8080 -s .",
    "dev": "serve -l 8080 -s ."
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
